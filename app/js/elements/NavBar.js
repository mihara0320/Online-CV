const NavBar = () => {

    let el = document.createElement('div');
    el.id = "navBar"
    el.class = "navBar"

    Object.assign(el, {
        init: () => {
        },
    })

    return el
}

export {NavBar}
